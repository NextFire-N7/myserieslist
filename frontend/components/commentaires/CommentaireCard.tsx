import { Commentaire } from "../../utils/types";

export default function CommentaireCard({ commentaire }: { commentaire: Commentaire }) {

  return(
    <div>
      <p>
        ok
      </p>
    </div>
  );
}